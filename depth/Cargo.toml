[workspace]
# The wasm module this package ships. It builds into
# target/wasm32-wasip2/release/, which is where the package's lib SQL names
# it.
members = [
    "depth",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
strip = true
