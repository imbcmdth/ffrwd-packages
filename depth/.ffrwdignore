# Build output. The file the lib SQL names is pulled back in by the
# manifest's closure; the rest of the directory stays out of the archive.
target/

# The wit the crate's build.rs copies in, which is the sidecar's own. The
# crate's own world lives in wit-world/ and ships.
wit/

# Local development state, not package content.
ffrwd.lock
